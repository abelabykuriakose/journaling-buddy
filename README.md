# journaling-buddy
Build a "Journaling Buddy." Create a simple chat loop that saves key facts about the user (name, hobbies, mood) to a local SQLite database and retrieves them dynamically in future chat sessions.
